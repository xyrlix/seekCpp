#include "store_helper.h"

FileHelper::FileHelper(const std::string& filename) {
    // code to open file
}

FileHelper::~FileHelper() {
    // code to close file
}

bool FileHelper::save(const std::string& data) {
    // code to save data to file
    return true;
}

bool FileHelper::load(std::string& data) {
    // code to load data from file
    return true;
}